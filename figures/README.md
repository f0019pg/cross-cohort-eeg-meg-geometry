# Figure generation

Run `python figures/run_all_figures.py` from the repository root. Each script reads committed numerical data from `source_data/` and `results/reported/` and writes vector and high-resolution raster files to `generated_figures/`.

The plotting scripts do not recompute participant-level analyses or substitute hard-coded display values for the reported result files.
